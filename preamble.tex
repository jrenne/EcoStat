\usepackage{booktabs}
\usepackage{amsthm}
\makeatletter
\def\thm@space@setup{%
  \thm@preskip=8pt plus 2pt minus 4pt
  \thm@postskip=\thm@preskip
}
\makeatother


%\usepackage[legalpaper, margin=1in]{geometry}

\usepackage[a4paper, total={6.5in, 9in}]{geometry}


% Tables: width-safe regression output and multi-page statistical tables.
\usepackage{longtable}
\usepackage{array}
\usepackage{adjustbox}
\usepackage{caption}
\captionsetup[table]{font=small,labelfont=bf,skip=6pt}
\setlength{\LTcapwidth}{\textwidth}
\usepackage{float}

% Avoid stretching vertical space around tables on sparsely filled pages.
\raggedbottom

% Keep enough room for a pilot exercise before starting its statement.
\usepackage{needspace}
